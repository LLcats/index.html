<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Invitación (nivel chistoso) 🍔💘</title>
  <style>
    :root{
      --bg1:#0f1226;
      --bg2:#1b0f1f;
      --card:#11152aee;
      --text:#f4f4ff;
      --muted:#c9c9ffcc;
      --accent:#ff4d8d;
      --accent2:#7c5cff;
      --yes:#35d07f;
      --no:#ff5a5f;
      --shadow: 0 18px 55px rgba(0,0,0,.45);
      --radius: 20px;
    }

    *{box-sizing:border-box}
    body{
      margin:0;
      min-height:100vh;
      display:grid;
      place-items:center;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Helvetica Neue", sans-serif;
      color:var(--text);
      background:
        radial-gradient(1100px 700px at 20% 10%, rgba(255,77,141,.20), transparent 60%),
        radial-gradient(1000px 650px at 85% 85%, rgba(124,92,255,.18), transparent 55%),
        linear-gradient(135deg, var(--bg1), var(--bg2));
      overflow:hidden;
      padding:18px;
    }

    .hearts{
      position:fixed;
      inset:0;
      pointer-events:none;
      opacity:.55;
    }
    .heart{
      position:absolute;
      font-size:22px;
      animation: floatUp linear infinite;
      transform: translateY(110vh);
    }
    @keyframes floatUp{
      from { transform: translateY(110vh) translateX(0) rotate(0deg); opacity:0; }
      15% { opacity:.9; }
      to   { transform: translateY(-20vh) translateX(var(--drift)) rotate(20deg); opacity:0; }
    }

    /* Layout móvil-first */
    .wrap{
      width:min(520px, 100%);
      display:flex;
      flex-direction:column;
      gap:14px;
    }

    .card{
      background: var(--card);
      border: 1px solid rgba(255,255,255,.08);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      padding: 18px;
      position:relative;
      overflow:hidden;
    }

    .card:before{
      content:"";
      position:absolute;
      inset:-2px;
      background:
        radial-gradient(520px 220px at 20% 0%, rgba(255,77,141,.22), transparent 60%),
        radial-gradient(500px 220px at 90% 10%, rgba(124,92,255,.20), transparent 55%);
      opacity:.9;
      pointer-events:none;
    }

    .content{
      position:relative;
      z-index:1;
      display:flex;
      flex-direction:column;
      gap:12px;
    }

    .pill{
      display:inline-flex;
      align-items:center;
      gap:10px;
      width:fit-content;
      padding:8px 12px;
      border-radius:999px;
      background: rgba(255,255,255,.06);
      border: 1px solid rgba(255,255,255,.10);
      color: var(--muted);
      font-size: 13px;
      letter-spacing:.2px;
    }

    h1{
      margin:0;
      font-size: clamp(26px, 7vw, 36px);
      line-height:1.05;
      letter-spacing:-.6px;
    }

    .sub{
      margin:0;
      color: var(--muted);
      font-size: 15px;
      line-height:1.5;
    }

    .banner{
      border-radius: 18px;
      border: 1px solid rgba(255,255,255,.08);
      overflow:hidden;
      position:relative;
      min-height: 170px;
      background:
        radial-gradient(900px 500px at 30% 30%, rgba(255,77,141,.26), transparent 60%),
        radial-gradient(800px 520px at 80% 80%, rgba(124,92,255,.24), transparent 55%),
        linear-gradient(135deg, rgba(255,255,255,.06), rgba(255,255,255,.03));
    }

    .banner .inner{
      position:absolute;
      inset:0;
      display:grid;
      place-items:center;
      padding:16px;
      text-align:center;
      gap:6px;
    }

    .big{
      font-size: clamp(52px, 12vw, 78px);
      margin:0;
      line-height:1;
      filter: drop-shadow(0 16px 28px rgba(0,0,0,.35));
    }

    .note{
      margin:0;
      color: var(--muted);
      font-size: 14px;
      line-height:1.45;
      max-width: 40ch;
    }

    .details{
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap:10px;
      margin-top:4px;
    }
    @media (max-width: 420px){
      .details{ grid-template-columns: 1fr; }
    }

    .detail{
      padding:12px 12px;
      border-radius: 16px;
      background: rgba(255,255,255,.05);
      border: 1px solid rgba(255,255,255,.08);
    }
    .detail b{
      display:block;
      font-size: 12.5px;
      color: var(--muted);
      margin-bottom:6px;
      font-weight:650;
      letter-spacing:.2px;
    }
    .detail span{ font-size: 15px; }

    .cta{
      display:flex;
      gap:10px;
      flex-wrap:wrap;
      align-items:center;
      margin-top:6px;
    }

    button{
      border:0;
      cursor:pointer;
      padding: 12px 14px;
      border-radius: 14px;
      font-weight:800;
      letter-spacing:.2px;
      transition: transform .12s ease, box-shadow .12s ease, background .12s ease;
      user-select:none;
      -webkit-tap-highlight-color: transparent;
    }
    button:active{ transform: translateY(1px) scale(.99); }

    .yes{
      background: linear-gradient(135deg, rgba(53,208,127,1), rgba(53,208,127,.78));
      box-shadow: 0 12px 26px rgba(53,208,127,.26);
      color:#07100b;
      flex: 1 1 160px;
    }

    .no{
      background: linear-gradient(135deg, rgba(255,90,95,1), rgba(255,90,95,.78));
      box-shadow: 0 12px 26px rgba(255,90,95,.20);
      color:#1a0607;
      flex: 0 0 auto;
    }

    .ghost{
      background: rgba(255,255,255,.06);
      border: 1px solid rgba(255,255,255,.12);
      color: var(--text);
      font-weight:750;
      flex: 1 1 160px;
    }

    .result{
      padding:14px 14px;
      border-radius: 18px;
      background: rgba(255,255,255,.05);
      border: 1px dashed rgba(255,255,255,.18);
      min-height: 72px;
      display:flex;
      align-items:center;
      gap:12px;
    }
    .badge{
      width:40px;height:40px;
      display:grid;place-items:center;
      border-radius: 14px;
      background: rgba(255,255,255,.08);
      border: 1px solid rgba(255,255,255,.12);
      font-size: 18px;
      flex: 0 0 auto;
    }
    .result p{
      margin:0;
      font-weight:750;
      line-height:1.3;
    }
    .result small{
      display:block;
      color: var(--muted);
      font-weight:600;
      margin-top:3px;
    }

    .footer{
      color: rgba(255,255,255,.42);
      font-size: 12.5px;
      line-height:1.35;
    }

    .tiny{
      font-size: 12px;
      color: rgba(255,255,255,.55);
    }
  </style>
</head>

<body>
  <div class="hearts" aria-hidden="true" id="hearts"></div>

  <main class="wrap">
    <div class="banner">
      <div class="inner">
        <p class="big">🍟💘</p>
        <p class="note">Se abre convocatoria oficial para: <b>comer rico</b> y reírse un rato. Cupos: 1 (idealmente tú).</p>
        <p class="tiny">*Nota legal: si aceptas, te conviertes en mi persona favorita por 24 horas (renovable).* 😌</p>
      </div>
    </div>

    <section class="card">
      <div class="content">
        <div class="pill">📩 Invitación con 0% vergüenza y 100% intención</div>
        <h1>¿Te atreves o le tienes miedo al éxito?</h1>
        <p class="sub">
          Yo pongo la conversación, tú pones lo más importante: <b>tu belleza y presencia</b> ✨
        </p>

        <div class="details">
          <div class="detail">
            <b>📍 Lugar</b>
            <span id="placeText">Cafetería de la Gata Horacia</span>
          </div>
          <div class="detail">
            <b>🕒 Hora</b>
            <span id="timeText">14:00 hrs</span>
          </div>
          <div class="detail">
            <b>📅 Día</b>
            <span id="dayText">Jueves</span>
          </div>
          <div class="detail">
            <b>🍔 Misión</b>
            <span id="planText">Comida + gatos + postre + risas + sutro</span>
          </div>
        </div>

        <div class="cta">
          <button class="yes" id="yesBtn">Aceptas o eri hetereke ✅</button>
          <button class="no" id="noBtn">No ❌</button>
</div>

        <div class="result" id="resultBox">
          <div class="badge">🤝</div>
          <div>
            <p>Esperando tu veredicto…</p>
            <small>Dato: el botón “No” tiene tendencias a huir (no lo juzgues).</small>
          </div>
        </div>
<div class="footer">
          Tip móvil: abre este archivo en tu celular y listo.
        </div>
      </div>
    </section>
  </main>

  <script>
    // ====== Corazoncitos flotantes ======
    const hearts = document.getElementById("hearts");
    const emojis = ["💖","💗","💘","💞","💕","💓","❤️‍🔥","✨","🍕","🍔","🌮","🍟"]; 
    const count = 16;

    for(let i=0;i<count;i++){
      const s = document.createElement("div");
      s.className = "heart";
      s.textContent = emojis[Math.floor(Math.random()*emojis.length)];
      s.style.left = Math.random()*100 + "vw";
      s.style.animationDuration = (8 + Math.random()*10) + "s";
      s.style.animationDelay = (Math.random()*6) + "s";
      s.style.setProperty("--drift", ((Math.random()*2-1)*60) + "px");
      s.style.fontSize = (16 + Math.random()*18) + "px";
      hearts.appendChild(s);
    }

    // ====== Elementos ======
    const yesBtn = document.getElementById("yesBtn");
    const noBtn = document.getElementById("noBtn");
    

    const placeText = document.getElementById("placeText");
    const timeText  = document.getElementById("timeText");
    const dayText   = document.getElementById("dayText");
    const planText  = document.getElementById("planText");

    const resultBox = document.getElementById("resultBox");

    function setResult(icon, title, subtitle){
      resultBox.innerHTML = `
        <div class="badge">${icon}</div>
        <div>
          <p>${title}</p>
          <small>${subtitle}</small>
        </div>
      `;
    }

    // ====== Botón Sí ======
    yesBtn.addEventListener("click", () => {
      setResult("😎", "¡Listo! Quedaste oficialmente invitad@.", "Jueves, 14:00 hrs en la Cafetería de la Gata Horacia. Llega con hambre y ganas de reír.");
      popConfetti();
    });

    // ====== Botón No (móvil-friendly: se mueve al tocar / acercar) ======
    const moveEvents = ["mouseenter", "touchstart", "click"];
    moveEvents.forEach(ev => noBtn.addEventListener(ev, (e) => {
      // Evita el zoom/doble tap raro en algunos navegadores
      if (e.cancelable) e.preventDefault();
      moveNoButton();
    }, { passive:false }));

    function moveNoButton(){
      const maxX = window.innerWidth - noBtn.offsetWidth - 18;
      const maxY = window.innerHeight - noBtn.offsetHeight - 18;
      const x = Math.max(10, Math.floor(Math.random()*maxX));
      const y = Math.max(10, Math.floor(Math.random()*maxY));

      noBtn.style.position = "fixed";
      noBtn.style.left = x + "px";
      noBtn.style.top  = y + "px";

      setResult("😈", "Ese “No” no está disponible por el momento.", "Intenta con “Sí” (viene con papitas incluidas).");
    }
// ====== Mini confetti ======
    function popConfetti(){
      const pieces = 60;
      for(let i=0;i<pieces;i++){
        const p = document.createElement("div");
        p.style.position = "fixed";
        p.style.left = (window.innerWidth/2 + (Math.random()*120-60)) + "px";
        p.style.top  = (window.innerHeight/2 + (Math.random()*60-30)) + "px";
        p.style.width = (6 + Math.random()*6) + "px";
        p.style.height = (10 + Math.random()*10) + "px";
        p.style.background = `hsl(${Math.random()*360}, 90%, 65%)`;
        p.style.borderRadius = "3px";
        p.style.zIndex = 9999;
        p.style.opacity = 0.95;
        document.body.appendChild(p);

        const dx = (Math.random()*2-1) * 240;
        const dy = (Math.random()*-1) * 280 - 110;
        const rot = (Math.random()*2-1) * 320;

        p.animate([
          { transform: "translate(0,0) rotate(0deg)", opacity: 1 },
          { transform: `translate(${dx}px, ${dy}px) rotate(${rot}deg)`, opacity: 0 }
        ], {
          duration: 1050 + Math.random()*450,
          easing: "cubic-bezier(.2,.9,.2,1)"
        });

        setTimeout(()=>p.remove(), 1600);
      }
    }
  </script>
</body>
</html>
